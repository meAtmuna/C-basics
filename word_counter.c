# include <stdio.h>
# include <stdlib.h>

#define lINE_LENGTH 1024

int main() {
    FILE *filePointer = fopen("repeated.txt", "r");

    char buffer[lINE_LENGTH];
    int lines = 0, words = 0;
    char lastChar = ' ';

    return 0;
}